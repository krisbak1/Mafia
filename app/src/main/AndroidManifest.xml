<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.mafia">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_moon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_moon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar.FullScreen"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name="com.example.mafia.PlayerPicker.PlayerActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.mafia.RolePicker.RoleRandomActivity" />
        <activity
            android:name="com.example.mafia.GameScreen.GameScreenActivity"/>
    </application>

</manifest>